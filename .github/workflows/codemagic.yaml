workflows:
  android-workflow:
    name: Android Workflow

    environment:
      java: 17

    scripts:
      - chmod +x gradlew
      - ./gradlew assembleDebug

    artifacts:
      - app/build/outputs/**/*.apk
